// Code generated by gogram/cmd/gen; DO NOT EDIT.

package gogram

import (
    "mime/multipart"
    "encoding/json"
    "bytes"
    "context"
    "strconv"
    "io"
)

{{ $root := . }}

{{ range .Methods }}
    {{ $method := toTitle .Name }}
    {{ $params := print $method "Params" }}
    {{ $option := print $method "Option" }}

    // {{ $params }} contains parameters for Client.{{ $method }}.
    type {{ $params }} struct {
        {{- template "fields.gotmpl" .Params }}
    }

    // {{ $option }} configures {{ $params }}.
    type {{ $option }} = Option[{{ $params }}]

    // Option applies one or more {{ $option }} values and returns the last rollback option.
    func (r *{{ $params }}) Option(opts ...{{ $option }}) (previous {{ $option }}) {
        for _, opt := range opts {
            previous = opt(r)
        }
        return previous
    }

    {{ range .Params }}
        {{ $param := toTitle .Name }}
        // With{{ $method }}{{ $param }} sets the {{ $param }} field.
        // {{ .Desc }}
        func With{{ $method }}{{ $param }} (value {{ toType .Type .IsRequired }}) {{ $option }} {
            return fieldOption(value, func(params *{{ $params }}) *{{ toType .Type .IsRequired }} {
                return &params.{{ $param }}
            })
        }
    {{ end }}

    // {{ $method }} calls the {{ .Name }} Telegram Bot API method.
    // {{ .Desc }}
    func (c *Client) {{ $method }}(ctx context.Context, params *{{ $params }}) (ret {{ toType .Result false }}, err error) {
        {{- $multipart := .Multipart $root.Types }}
        {{- if $multipart }}
            reader, pw := io.Pipe()
            writer := multipart.NewWriter(pw)
            contentType := writer.FormDataContentType()
            writeErr := make(chan error, 1)

            go func() {
                var err error
                defer func() {
                    if err != nil {
                        _ = pw.CloseWithError(err)
                        writeErr <- err
                        return
                    }

                    err = writer.Close()
                    if err != nil {
                        _ = pw.CloseWithError(err)
                        writeErr <- err
                        return
                    }

                    writeErr <- pw.Close()
                }()
            {{ range .Params }}
                {{- $goTitle := toTitle .Name }}
                {{- $goType := toType .Type true }}

                {{ if eq $goType "int64" }}
                    {{ if not .IsRequired }} if params.{{ $goTitle }} != 0 {{ end }} {
                        v := strconv.FormatInt(params.{{ $goTitle }}, 10)
                        err = writer.WriteField("{{ .Name }}", v)
                        if err != nil {
                            return
                        }
                    }
                {{- else if eq $goType "string" }}
                    {{ if not .IsRequired }} if params.{{ $goTitle }} != "" {{ end }} {
                        v := params.{{ $goTitle }}
                        err = writer.WriteField("{{ .Name }}", v)
                        if err != nil {
                            return
                        }
                    }
                {{- else if eq $goType "bool" }}
                    {{ if not .IsRequired }} if params.{{ $goTitle }} {{ end }} {
                        v := strconv.FormatBool(params.{{ $goTitle }})
                        err = writer.WriteField("{{ .Name }}", v)
                        if err != nil {
                            return
                        }
                    }
                {{- else if eq $goType "float64" }}
                    {{ if not .IsRequired }} if params.{{ $goTitle }} != 0 {{ end }} {
                        v := strconv.FormatFloat(params.{{ $goTitle }}, 'f', -1, 64)
                        err = writer.WriteField("{{ .Name }}", v)
                        if err != nil {
                            return
                        }
                    }
                {{- else }}
                    {{- $uploadCode := .FileUploadCode $root.Types }}
                    {{ if and (not $uploadCode) (not .IsRequired) }}
                        {{- if eq (slice $goType 0 2) "[]" }}
                            if len(params.{{ $goTitle }}) != 0
                        {{- else }}
                            if params.{{ $goTitle }} != nil
                        {{- end }}
                    {{- end }} {
                    {{- if $uploadCode }}
                        {{- $uploadCode }}
                    {{- end }}
                    {{- if ne $goType "InputFile" }}
                        {{- /* Use pointer to trigger pointer-receiver MarshalJSON on union types. */}}
                        var bs []byte
                        bs, err = json.Marshal(&params.{{ $goTitle }})
                        if err != nil {
                            return
                        }

                        var p io.Writer
                        p, err = writer.CreateFormField("{{ .Name }}")
                       	if err != nil {
                      		return
                       	}

                        _, err = p.Write(bs)
                       	if err != nil {
                      		return
                       	}
                    {{- end }}
                    }
                {{- end }}
            {{- end }}

            }()
        {{- else }}
            buffer := new(bytes.Buffer)
           	if err = json.NewEncoder(buffer).Encode(params); err != nil {
          		return
           	}

           	reader := bytes.NewReader(buffer.Bytes())

            contentType := "application/json"
        {{ end }}

        var result json.RawMessage

        result, err = c.Raw(ctx, "{{ .Name }}", reader, contentType, nil)
        if err != nil {
            {{- if $multipart }}
                _ = reader.CloseWithError(err)
                <-writeErr
            {{- end }}
            return
        }
        {{- if $multipart }}
            if err = <-writeErr; err != nil {
                return
            }
        {{- end }}

        {{ toMake .Result "ret" "ref" }}

       	err = json.Unmarshal(result, ref)
        if err != nil {
            return
        }

       	return ret, err
    }
{{ end }}

func createFormFileFromInputFile(writer *multipart.Writer, f *InputFile, name string) error {
	if f.File == nil {
		return nil
	}

	var w io.Writer
	var err error

	f.fieldName = name

	w, err = writer.CreateFormFile(f.fieldName, f.FileName)
	if err != nil {
		return err
	}

	_, err = io.Copy(w, f.File)
	if err != nil {
		return err
	}

	if closer, ok := f.File.(io.Closer); ok {
		err = closer.Close()
		if err != nil {
			return err
		}
	}

	return nil
}
